# Nisria Command Center — server-only secrets (NEVER NEXT_PUBLIC, this app is admin).
# Set these in Vercel project env (Production + Preview).
SUPABASE_URL=https://ptvhqudonvvszupzhcfl.supabase.co
SUPABASE_SERVICE_KEY=__service_role_key__      # full DB access, server-side only
ADMIN_PASSWORD=__choose_a_strong_password__    # what Nur/Taona type at /login
SESSION_TOKEN=__long_random_string__           # cookie value set on successful login
